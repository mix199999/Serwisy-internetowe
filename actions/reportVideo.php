<?php
session_start();
echo 'vid';

$reason = $_POST['reason1'];
$title = $_POST['title1'];

if(isset($_POST['reason1']) and isset($_POST['title1']) ){

        //$videoId = $_POST['videoId'];
        //$userId = $_POST['userId'];
        
        
}
        

        //wszytko jest dowiedziec sie jakie tabele itp i co gdzie jest zapisane, tak samo mozna uzytkownika tylko łatwiej
        // videoID, reason, title, userId, status?
        
        //$stmt = $db->prepare("INSERT INTO reports (videoId, reason, title) VALUES (:videoId, :reason, :title)");
        //$stmt->bindParam(':videoId', $videoId);
        //itd...
        
    
